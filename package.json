{
  "name": "viral-script-engine",
  "version": "1.0.0",
  "description": "AI-powered viral script generator backend",
  "scripts": {
    "start": "cd backend && npm install && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
